malarkey(document.querySelector('.breve'), {
    typeSpeed: 50,
    deleteSpeed: 50,
    pauseDelay: 2000,
    loop: true,
    postfix: ''
  })
  .type('web').pause().delete()
  .type('gráfico').pause().delete()
  .type('3D').pause().delete()
  .type('digital').pause().delete()
 
